{
  "name": "open-ci",
  "version": "1.0.1",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "packageManager": "npm@11.14.1",
  "scripts": {
    "build": "node scripts/build-action.mjs",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test src/*.test.ts",
    "verify": "npm run check && npm test"
  },
  "dependencies": { "yaml": "2.9.1" },
  "devDependencies": {
    "@types/node": "24.13.6",
    "esbuild": "0.28.2",
    "typescript": "7.0.2"
  }
}
